name: Warm cache from sitemap Salomon

on:
  schedule:
    - cron: '0 * * * *'   # запуск раз в час
  workflow_dispatch:

jobs:
  warm:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python deps
        run: python -m pip install --upgrade pip && python -m pip install requests

      - name: Warm cache from sitemap
        env:
          MAIN_SITEMAP: "https://salomon.community/sitemap.xml"  # при необходимости замени
          DELAY: "0"  # пауза между запросами (сек.)
        run: |
          python - <<'PY'
          import requests, time, xml.etree.ElementTree as ET, re, os

          MAIN = os.environ.get("MAIN_SITEMAP")
          DELAY = float(os.environ.get("DELAY", "2"))
          headers = {"User-Agent": 
                  "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"
                  "AppleWebKit/537.36 (KHTML, like Gecko) "
                  "Chrome/138.0.0.0 Safari/537.36"
          }

          def fetch(url):
              try:
                  r = requests.get(url, headers=headers, timeout=30)
                  r.raise_for_status()
                  return r.content
              except Exception as e:
                  print("❌ Error fetching:", url, e)
                  return None

          def extract_locs(bts):
              urls = []
              try:
                  root = ET.fromstring(bts)
                  for el in root.findall('.//{*}loc'):
                      if el.text:
                          urls.append(el.text.strip())
              except Exception as e:
                  print("XML parse error:", e)
              return urls

          def is_valid_page(url):
              url_lower = url.lower()
              if re.search(r'\.(jpg|jpeg|png|gif|webp|svg|avif|pdf|mp4|webm|xml|xsl)$', url_lower):
                  return False
              if any(x in url_lower for x in ['feed', 'wp-json', '?', '#']):
                  return False
              return True

          print("🛰 Fetching main sitemap:", MAIN)
          content = fetch(MAIN)
          if not content:
              print("Failed to fetch main sitemap.")
              raise SystemExit(1)

          # находим вложенные сайтмапы
          try:
              root = ET.fromstring(content)
              nested = [el.text.strip() for el in root.findall('.//{*}sitemap/{*}loc') if el.text]
          except Exception as e:
              print("Parse error:", e)
              nested = []

          all_urls = []

          if nested:
              print(f"🔍 Found {len(nested)} nested sitemap(s).")
              for sm in nested:
                  print("  →", sm)
                  b = fetch(sm)
                  if not b:
                      continue
                  locs = extract_locs(b)
                  all_urls.extend(locs)
          else:
              locs = extract_locs(content)
              all_urls.extend(locs)

          filtered = [u for u in all_urls if is_valid_page(u)]
          filtered = sorted(set(filtered))
          print(f"✅ Total pages to warm: {len(filtered)}")

          warmed = 0
          for url in filtered:
              try:
                  r = requests.get(url, headers=headers, timeout=15)
                  print(f"🔥 {r.status_code} {url}")
                  warmed += 1
                  time.sleep(DELAY)
              except Exception as e:
                  print(f"⚠️ Failed {url}: {e}")

          print(f"🏁 Done! Warmed {warmed}/{len(filtered)} pages.")
          PY
